describe('tacticalService', function() {

  beforeEach(module('tactical'));

  it('should ...', inject(function(tacticalService) {

    //expect(tacticalService.doSomething()).toEqual('something');

  }));

});
